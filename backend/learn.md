## step 1. node.js installation 

    - https://nodejs.org/en/download


## step 2. VS code dev setup 

- Created backend name folder 
- open with vs code 
- npm -v check in terminal 
- node -v
- 

## step-3: Backend creation with Node and express js

- npm init
- Above command creates package.json and it contains all dependencies for backend which we will create
- created index.js file which will be the main entry point



# Step 4 How tp run backend or normal javascriptfile with the help of node

- difference between node and nodemon
    to run normal file we need node.js and to see continues changes we installed nodemon 
    with the help of comand npm i modemon -g

- we run code for normal js file :node file path 
- so we run backend :

        - we write a script in a package.json
        - we wrote two scripts one for dev and one for start 
        - to run the scripts we go the main folder in terminal where package.json exist in npm run --scriptkey--

# Step 5 Installation of Express.js for creating backend server 

- npm i express
- so above comand installs express for backend dseerver dependancies we can check it in package.json
- after installation we have imported express module in index.js which is main entry point.
- import lies in main top of the file 


Sequece of line in index.js 
3, 8, 15

    we sent request to server using send method 
    we use get to get message on http 
    to get output in browser localhost:port_no
